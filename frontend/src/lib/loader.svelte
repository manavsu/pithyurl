<style>
    .loader {
      animation: breathe 2s infinite ease-in-out;
    }
  
    @keyframes breathe {
      0%, 100% {
        transform: scale(1);
      }
      50% {
        transform: scale(1.5);
      }
    }
  </style>
  
  <div class="loader rounded-full bg-main h-full w-full"></div>